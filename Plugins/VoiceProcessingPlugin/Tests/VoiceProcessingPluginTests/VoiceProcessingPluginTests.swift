import XCTest
@testable import VoiceProcessingPlugin

final class VoiceProcessingPluginTests: XCTestCase {
    func testExample() throws {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(VoiceProcessingPlugin().text, "Hello, World!")
    }
}
