import XCTest
@testable import GaiaPluginManager

final class GaiaPluginManagerTests: XCTestCase {
    func testExample() throws {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(GaiaPluginManager().text, "Hello, World!")
    }
}
